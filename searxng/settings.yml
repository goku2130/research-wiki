# Minimal SearXNG config for programmatic use by the wiki agent.
# JSON format is enabled so the agent can call /search?q=...&format=json
use_default_settings: true

server:
  # Local-only instance; change if you expose it beyond localhost.
  secret_key: "f2295d8673e09d2eaa68d0681f8c6ab0828cc30702595c320674a65f72c03be0"
  limiter: false
  image_proxy: false

search:
  safe_search: 0
  formats:
    - html
    - json

engines:
  - name: arxiv
    disabled: false
